# CHANGELOG — research-project

Версия шаблона = номер верхней записи. Растёт на +1 при изменении, которое стоит распространять в проекты, инстанцированные из этого шаблона; мелкие правки формулировок версию не двигают. Теги: `[base]` — из общего скелета base-template, `[research]` — собственная специфика класса.

Выровнен с **base-template@v1**.

## v1 — 2026-06-10
Учредительная версия: шаблон стал версионируемой единицей (версия + этот changelog + сверка обновлений на lint). Зафиксировано состояние после выравнивания механики по base-template; домен — центральный тип `claims/` с evidence chain, «источники побеждают вики», локализация цитат.
